# Pin/Pengumuman Teras — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Admin menyematkan tepat satu kiriman sebagai pengumuman di puncak feed Teras, dengan badge "📌 Disematkan" dan dedup dari alur kronologis.

**Architecture:** Kolom `pinned_at` di `community_posts` (+ index parsial unik satu-pin, migrasi manual user), helper kelayakan murni `lib/community-pin.js`, endpoint `POST`/`DELETE /api/community/posts/:id/pin` (admin-only), halaman pertama feed utama membawa field `pinned` yang dipetakan lewat mapper baris feed yang SAMA, FE merender kartu pin lewat jalur kartu yang ada (prepend + dedup render).

**Tech Stack:** Express 5 + Supabase (PostgREST), React 18 + TS (Vite), `node:test`.

**Spec:** `docs/superpowers/specs/2026-07-22-teras-pin-design.md`

## Global Constraints

- Hak pin: `req.user.role === 'admin'` → selain itu 403 `Hanya admin yang bisa menyematkan`. BUKAN `canModerateCommunityContent`.
- Slot tunggal: pin baru melepas pin lama (UPDATE semua `pinned_at IS NOT NULL` → NULL dulu); race index unik 23505 → 409 `Pin sedang diubah, coba lagi`.
- Kelayakan: hanya kiriman induk (`parent_post_id` NULL, `is_reply` false), hidup. Pesan: `Kiriman tidak ditemukan` (deleted) / `Balasan tidak bisa disematkan` / `Hanya segmen pertama utas yang bisa disematkan`. Nilai `undefined` (pra-migrasi-thread) = lolos.
- Pra-migrasi `pinned_at`: SEMUA akses ber-guard `isCommunityPinSchemaMissing` (42703/PGRST204 + /pinned_at/); UPDATE gagal → 503 `Migrasi pin Teras belum diterapkan`; feed degradasi `pinned: null` senyap.
- PELAJARAN FITUR EDIT (mengikat): kolom baru wajib sampai ke BUILDER PAYLOAD respons, bukan hanya select. Field `pinned` feed dipetakan lewat mapper baris yang sama dengan `data` (di-refactor jadi fungsi lokal, bukan duplikasi blok).
- Unpin idempoten (unpin kiriman tak-dipin → 200).
- FE: blok pin hanya linimasa utama (`!profileSlug && !postId`); dedup di render (filter id), query/cursor TIDAK berubah; badge kecil "Disematkan" tampil di kartu ber-`pinned_at` di mana pun.
- Tanpa notifikasi, tanpa pin profil, tanpa multi-pin, tanpa riwayat.
- Gate: `node --check server.js`; `npx tsc --noEmit` tanpa error baru di file tersentuh; `npm run build:spa`; JANGAN suite penuh/browser test.
- Sebelum SETIAP commit: `git branch --show-current` = `main`; sesi lain commit ke main bersamaan — cari anchor by content, stage hanya file task.
- Artefak SDD pakai nama file UNIK berprefix `teras-pin-` (sesi paralel memakai `.superpowers/sdd/task-N-*` yang sama).

---

### Task 1: Helper murni `lib/community-pin.js` + unit test

**Files:**
- Create: `lib/community-pin.js`
- Test: `tests/community-pin.test.js`

**Interfaces:**
- Consumes: — (murni, tanpa dependensi)
- Produces (dipakai Task 2): `canPinCommunityPost(post) → { ok: true } | { ok: false, error }` dengan `post = { deleted_at, parent_post_id, is_reply }`.

- [ ] **Step 1: Tulis unit test yang gagal**

Buat `tests/community-pin.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { canPinCommunityPost } from '../lib/community-pin.js';

test('kiriman induk hidup boleh dipin', () => {
  assert.deepEqual(
    canPinCommunityPost({ deleted_at: null, parent_post_id: null, is_reply: false }),
    { ok: true },
  );
});

test('kiriman terhapus ditolak', () => {
  assert.deepEqual(
    canPinCommunityPost({ deleted_at: '2026-07-22T00:00:00Z', parent_post_id: null, is_reply: false }),
    { ok: false, error: 'Kiriman tidak ditemukan' },
  );
});

test('balasan ditolak', () => {
  assert.deepEqual(
    canPinCommunityPost({ deleted_at: null, parent_post_id: 'p1', is_reply: true }),
    { ok: false, error: 'Balasan tidak bisa disematkan' },
  );
});

test('segmen lanjutan utas ditolak', () => {
  assert.deepEqual(
    canPinCommunityPost({ deleted_at: null, parent_post_id: 'p1', is_reply: false }),
    { ok: false, error: 'Hanya segmen pertama utas yang bisa disematkan' },
  );
});

test('pra-migrasi thread (kolom undefined) diperlakukan lolos', () => {
  assert.deepEqual(canPinCommunityPost({ deleted_at: null }), { ok: true });
});

test('urutan cek: deleted menang atas balasan', () => {
  assert.deepEqual(
    canPinCommunityPost({ deleted_at: '2026-07-22T00:00:00Z', parent_post_id: 'p1', is_reply: true }),
    { ok: false, error: 'Kiriman tidak ditemukan' },
  );
});
```

- [ ] **Step 2: Jalankan test, pastikan GAGAL**

Run: `node --test tests/community-pin.test.js`
Expected: FAIL — `Cannot find module '../lib/community-pin.js'`

- [ ] **Step 3: Implementasi `lib/community-pin.js`**

```js
// Kelayakan pin kiriman Teras — murni, tanpa DB, diuji unit.
// Kebijakan (spec 2026-07-22): hanya kiriman induk yang hidup. Utas dipin
// lewat segmen pertamanya; balasan & segmen lanjutan ditolak. Nilai
// undefined pada kolom utas (pra-migrasi is_reply/parent_post_id) lolos —
// konsisten dengan degradasi fitur utas di server.

export function canPinCommunityPost(post) {
  if (post?.deleted_at) return { ok: false, error: 'Kiriman tidak ditemukan' };
  if (post?.is_reply === true) return { ok: false, error: 'Balasan tidak bisa disematkan' };
  if (post?.parent_post_id !== null && post?.parent_post_id !== undefined) {
    return { ok: false, error: 'Hanya segmen pertama utas yang bisa disematkan' };
  }
  return { ok: true };
}
```

- [ ] **Step 4: Jalankan test, pastikan LOLOS**

Run: `node --test tests/community-pin.test.js`
Expected: PASS (6 test)

- [ ] **Step 5: Commit**

```bash
git branch --show-current   # harus: main
git add lib/community-pin.js tests/community-pin.test.js
git commit -m "feat(teras): helper murni kelayakan pin kiriman"
```

---

### Task 2: server.js — endpoint pin/unpin + `pinned_at` & field `pinned` di feed/detail

**Files:**
- Modify: `server.js` — (a) import; (b) helper schema-missing dekat `isCommunityEditSchemaMissing`; (c) dua endpoint baru tepat di atas `app.delete('/api/community/posts/:id', ...)`; (d) feed handler `GET /api/community/feed` (select flag + loop retry + refactor mapper + query pinned + respons); (e) detail handler `GET /api/community/posts/:id` (select flag + loop + payload).

**Interfaces:**
- Consumes (Task 1): `canPinCommunityPost(post)`.
- Consumes (server.js, ada): `getAgentById`, `requireCommunityAccess`, `isCommunityUuid`, `isCommunityThreadSchemaMissing`, `supabase`, `loadCommunityEngagementMaps`.
- Produces (dipakai Task 3): `POST /api/community/posts/:id/pin` → 200 `{ data: { id, pinned_at } }` | 403/404/400/409/503 `{error}`; `DELETE .../pin` → 200 `{ data: { id, pinned_at: null } }` (idempoten); respons feed halaman pertama non-profil dapat field `pinned: <payload baris feed> | null`; payload feed & detail dapat `pinned_at: string | null`.

- [ ] **Step 1: Import + helper**

Di blok import lib: `import { canPinCommunityPost } from './lib/community-pin.js';`

Setelah `isCommunityEditSchemaMissing`:

```js
function isCommunityPinSchemaMissing(error) {
  const code = String(error?.code || '');
  if (!['42703', 'PGRST204'].includes(code)) return false;
  return /pinned_at/i.test(String(error?.message || error?.details || ''));
}
```

- [ ] **Step 2: Endpoint pin & unpin**

Tepat di atas `app.delete('/api/community/posts/:id', ...)`; gaya catch = tetangga:

```js
// POST /api/community/posts/:id/pin — sematkan SATU kiriman (admin).
// Pin baru melepas pin lama; index parsial unik di DB menegakkan slot tunggal.
app.post('/api/community/posts/:id/pin', authMiddleware, async (req, res) => {
  try {
    const agent = await getAgentById(req.user.id);
    if (!agent) return res.status(404).json({ error: 'Agent not found' });
    if (!requireCommunityAccess(agent, res)) return;
    if (req.user.role !== 'admin') {
      return res.status(403).json({ error: 'Hanya admin yang bisa menyematkan' });
    }
    if (!isCommunityUuid(req.params.id)) {
      return res.status(404).json({ error: 'Kiriman tidak ditemukan' });
    }
    const loadPostForPin = withThread => supabase
      .from('community_posts')
      .select(`id, deleted_at${withThread ? ', parent_post_id, is_reply' : ''}`)
      .eq('id', req.params.id)
      .maybeSingle();
    let { data: post, error: findError } = await loadPostForPin(true);
    if (isCommunityThreadSchemaMissing(findError)) {
      ({ data: post, error: findError } = await loadPostForPin(false));
    }
    if (findError) throw findError;
    if (!post) return res.status(404).json({ error: 'Kiriman tidak ditemukan' });
    const verdict = canPinCommunityPost(post);
    if (!verdict.ok) {
      return res
        .status(verdict.error === 'Kiriman tidak ditemukan' ? 404 : 400)
        .json({ error: verdict.error });
    }

    const { error: clearError } = await supabase
      .from('community_posts')
      .update({ pinned_at: null })
      .not('pinned_at', 'is', null);
    if (isCommunityPinSchemaMissing(clearError)) {
      return res.status(503).json({ error: 'Migrasi pin Teras belum diterapkan' });
    }
    if (clearError) throw clearError;

    const pinnedAt = new Date().toISOString();
    const { error: pinError } = await supabase
      .from('community_posts')
      .update({ pinned_at: pinnedAt })
      .eq('id', post.id);
    if (isCommunityPinSchemaMissing(pinError)) {
      return res.status(503).json({ error: 'Migrasi pin Teras belum diterapkan' });
    }
    if (String(pinError?.code || '') === '23505') {
      return res.status(409).json({ error: 'Pin sedang diubah, coba lagi' });
    }
    if (pinError) throw pinError;

    res.json({ data: { id: post.id, pinned_at: pinnedAt } });
  } catch (err) {
    console.error('[community] pin error:', err);
    res.status(500).json({ error: 'Gagal menyematkan kiriman' });
  }
});

// DELETE /api/community/posts/:id/pin — lepas sematan (admin, idempoten).
app.delete('/api/community/posts/:id/pin', authMiddleware, async (req, res) => {
  try {
    const agent = await getAgentById(req.user.id);
    if (!agent) return res.status(404).json({ error: 'Agent not found' });
    if (!requireCommunityAccess(agent, res)) return;
    if (req.user.role !== 'admin') {
      return res.status(403).json({ error: 'Hanya admin yang bisa menyematkan' });
    }
    if (!isCommunityUuid(req.params.id)) {
      return res.status(404).json({ error: 'Kiriman tidak ditemukan' });
    }
    const { error: unpinError } = await supabase
      .from('community_posts')
      .update({ pinned_at: null })
      .eq('id', req.params.id);
    if (isCommunityPinSchemaMissing(unpinError)) {
      return res.status(503).json({ error: 'Migrasi pin Teras belum diterapkan' });
    }
    if (unpinError) throw unpinError;
    res.json({ data: { id: req.params.id, pinned_at: null } });
  } catch (err) {
    console.error('[community] unpin error:', err);
    res.status(500).json({ error: 'Gagal melepas sematan' });
  }
});
```

- [ ] **Step 3: Feed — select flag + mapper + field `pinned`**

Di `GET /api/community/feed`:

a) `buildPostsQuery` dapat parameter `includePin`; select disisipi `${includePin ? 'pinned_at, ' : ''}` tepat sebelum `is_system, `. Loop retry: `let includePin = true;`, naikkan batas attempt satu lagi (6 → 7, perbarui komentar hitungan), cabang:

```js
      if (includePin && isCommunityPinSchemaMissing(postsError)) {
        includePin = false;
        continue;
      }
```

b) **Refactor mapper** (WAJIB, bukan duplikasi): blok `.map(post => ({ ... }))` yang membangun payload baris feed diangkat jadi fungsi lokal `const toFeedPayload = post => ({ ... });` (isi persis objek yang ada — termasuk `edited_at`, `is_reply`, dan cabang `parent_author` mode profil — DITAMBAH `pinned_at: post.pinned_at ?? null,` setelah `edited_at`). `data` dibangun via `(posts || []).map(toFeedPayload)`.

c) Query pinned + respons — setelah engagement maps DIHITUNG, tapi id pinned harus masuk hitungan: fetch baris pinned SEBELUM `loadCommunityEngagementMaps`, lalu masukkan id-nya ke `postIds`:

```js
    // Kiriman tersemat: hanya halaman pertama linimasa utama. Query terpisah
    // (bukan bagian cursor) — dedup dilakukan klien saat render.
    let pinnedRow = null;
    if (!before && !profileMember && includePin) {
      const { data: row, error: pinnedError } = await supabase
        .from('community_posts')
        .select(`id, body, photo_url, ${includeMedia ? 'media, ' : ''}${includeQuote ? 'quoted_post_id, ' : ''}${includeLinkPreview ? 'link_preview, ' : ''}${includeThread ? 'parent_post_id, is_reply, ' : ''}pinned_at, ${includeEdited ? 'edited_at, ' : ''}is_system, created_at, agent_id, agent:agents!community_posts_agent_id_fkey(name, slug, photo)`)
        .not('pinned_at', 'is', null)
        .is('deleted_at', null)
        .maybeSingle();
      // Degradasi senyap: galat apa pun pada query pinned tidak boleh
      // menggagalkan feed.
      if (!pinnedError && row) pinnedRow = row;
    }
```

Letakkan `const postIds = ...` SETELAH blok itu dan gabungkan: `const postIds = [...(posts || []).map(post => post.id), ...(pinnedRow ? [pinnedRow.id] : [])];` (hapus duplikat dengan `Array.from(new Set(...))`). Respons:

```js
    res.json({
      success: true,
      data,
      next_cursor: nextCursor,
      ...(!before && !profileMember ? { pinned: pinnedRow ? toFeedPayload(pinnedRow) : null } : {}),
    });
```

(Catatan: `toFeedPayload` membaca map engagement — pastikan deklarasinya SETELAH engagement maps tersedia, seperti blok map yang ada sekarang.)

- [ ] **Step 4: Detail — select flag + payload**

Di `GET /api/community/posts/:id`: `buildPostQuery` dapat parameter `includePin`, select disisipi `${includePin ? 'pinned_at, ' : ''}` sebelum `is_system, `, loop retry ditambah flag + cabang `isCommunityPinSchemaMissing` (batas attempt naik satu, komentar diperbarui), dan builder payload detail ditambah `pinned_at: post.pinned_at ?? null,` setelah `edited_at`. Segmen utas & komentar TIDAK disentuh (tidak bisa dipin).

- [ ] **Step 5: Verifikasi**

Run: `node --check server.js` → bersih.
Run: `node --test tests/community-pin.test.js tests/community-edit.test.js` → semua PASS.

- [ ] **Step 6: Commit**

```bash
git branch --show-current   # harus: main
git add server.js
git commit -m "feat(teras): endpoint pin/unpin admin + pinned_at & field pinned di feed/detail"
```

---

### Task 3: FE — blok pin, dedup, menu admin, badge

**Files:**
- Modify: `src/components/TerasPage.tsx` — tipe (`CommunityPost`), state + handler (dekat `patchEntryBody`), konsumsi respons feed (~1898-1906), render daftar (`visiblePosts.map` ~4690-4700), menu titik-tiga (dekat item Edit), badge dekat `<time>`.

**Interfaces:**
- Consumes (Task 2): endpoint pin/unpin + field `pinned` feed + `pinned_at` payload (bentuk lihat Task 2). Pola file: `requestJson` envelope (`payload.data`), `getAuthHeaders`, `errorMessage`, `showToast`, ikon lucide.
- Produces: state `pinnedPost: CommunityPost | null`; handler `togglePin(post)`.

- [ ] **Step 1: Tipe + state**

`interface CommunityPost`: tambah `pinned_at?: string | null;` setelah `edited_at`. Dekat state feed:

```tsx
  // Kiriman tersemat (pengumuman admin) — dikirim server hanya di halaman
  // pertama linimasa utama; null = tidak ada pin.
  const [pinnedPost, setPinnedPost] = useState<CommunityPost | null>(null);
```

- [ ] **Step 2: Konsumsi respons feed**

Di fetch feed (setelah `const serverPosts = payload.data || [];`), hanya saat halaman pertama linimasa utama:

```tsx
      if (!before && !profileSlug) {
        const pinnedFromServer = (payload as { pinned?: CommunityPost | null }).pinned;
        setPinnedPost(pinnedFromServer ?? null);
      }
```

(Kalau `ApiEnvelope` mudah diperluas dengan field opsional `pinned`, lakukan itu alih-alih cast — ikuti bentuk deklarasi yang ada.)

- [ ] **Step 3: Render blok pin + dedup**

Di tempat `visiblePosts` dipakai untuk `.map` (linimasa utama saja — kondisi `!profileSlug && !postId`... gunakan variabel mode yang SUDAH ada di file, mis. `isDetailView`/`profileSlug`):

```tsx
  const showPinnedBlock = !profileSlug && !isDetailView && !!pinnedPost;
  const renderPosts = showPinnedBlock && pinnedPost
    ? [pinnedPost, ...visiblePosts.filter(post => post.id !== pinnedPost.id)]
    : visiblePosts;
```

Ganti sumber `.map` jadi `renderPosts`. Di dalam map, di atas kartu pertama saat ia kartu pin, render badge blok:

```tsx
{showPinnedBlock && index === 0 && post.id === pinnedPost?.id && (
  <div className="mb-1 flex items-center gap-1.5 text-[11.5px] font-semibold text-emerald-700 dark:text-emerald-400">
    <Pin size={13} /> Disematkan
  </div>
)}
```

(Tambah `Pin`, `PinOff` ke import lucide-react.) Kartu pin memakai jalur render kartu yang ada apa adanya — reaksi/komentar/kutip berfungsi normal. Interaksi state di kartu pin (mis. reaksi) beroperasi pada objek `pinnedPost` — cukup: saat `patchEntryBody`/pembaruan post terjadi berbasis `setPosts`, sinkronkan juga `setPinnedPost(current => current && current.id === id ? { ...current, ...patch } : current)` untuk body/edited_at (tambahkan di `patchEntryBody`).

- [ ] **Step 4: Menu admin + handler**

Handler dekat `patchEntryBody`:

```tsx
  const togglePin = async (post: CommunityPost) => {
    const isPinned = pinnedPost?.id === post.id;
    try {
      const result = await requestJson<{ id: string; pinned_at: string | null }>(
        `/api/community/posts/${encodeURIComponent(post.id)}/pin`,
        { method: isPinned ? 'DELETE' : 'POST', headers: getAuthHeaders() },
        isPinned ? 'Gagal melepas sematan' : 'Gagal menyematkan',
      );
      if (isPinned) {
        setPinnedPost(null);
        showToast('Sematan dilepas', 'success');
      } else {
        setPinnedPost({ ...post, pinned_at: result.data?.pinned_at ?? new Date().toISOString() });
        showToast('Kiriman disematkan', 'success');
      }
    } catch (error) {
      showToast(errorMessage(error, 'Gagal mengubah sematan'), 'error');
    }
  };
```

Item menu di menu titik-tiga kartu (di bawah item Edit), hanya linimasa utama + admin:

```tsx
                              {agent.role === 'admin' && !profileSlug && !isDetailView && (
                                <button
                                  type="button"
                                  role="menuitem"
                                  onClick={() => {
                                    closePostMenu(post.id, true);
                                    void togglePin(post);
                                  }}
                                  className="flex min-h-12 w-full items-center justify-between gap-3 px-4 text-left text-[13.5px] font-semibold text-gray-800 transition-colors hover:bg-gray-100 active:bg-gray-100 dark:text-slate-200 dark:hover:bg-slate-700/60 dark:active:bg-slate-700/60"
                                >
                                  {pinnedPost?.id === post.id ? 'Lepas sematan' : 'Sematkan'}
                                  {pinnedPost?.id === post.id ? <PinOff size={16} /> : <Pin size={16} />}
                                </button>
                              )}
```

(Sesuaikan mekanisme tutup-menu dengan yang dipakai item Edit/Hapus di situ.) Hapus kiriman: di handler sukses hapus kiriman, tambah `setPinnedPost(current => (current && current.id === postId ? null : current));`.

- [ ] **Step 5: Badge di detail**

Di dekat `<time>` kartu (samping label `· diedit`), untuk tampilan detail saja (blok pin utama sudah punya badge sendiri):

```tsx
{isDetailView && post.pinned_at && (
  <span className="flex shrink-0 items-center gap-1 text-[11px] font-medium text-emerald-700 dark:text-emerald-400"><Pin size={11} /> Disematkan</span>
)}
```

- [ ] **Step 6: Verifikasi**

Run: `npx tsc --noEmit 2>&1 | grep -iE "TerasPage|community-pin"` → kosong.
Run: `npm run build:spa` → sukses.

- [ ] **Step 7: Commit**

```bash
git branch --show-current   # harus: main
git add src/components/TerasPage.tsx
git commit -m "feat(teras): blok pin di feed + menu sematkan admin + badge disematkan"
```

---

### Task 4: Verifikasi akhir + review final + push + serah-terima

**Files:** — (verifikasi, review, rilis)

- [ ] **Step 1: Gate cepat**

```bash
node --check server.js && node --test tests/community-pin.test.js tests/community-edit.test.js tests/teras-draft.test.js && npx tsc --noEmit 2>&1 | grep -iE "TerasPage|community-pin" ; npm run build:spa
```

Expected: semua hijau. (JANGAN suite penuh/browser.)

- [ ] **Step 2: Review whole-branch final** (controller: reviewer paling mampu, paket diff path-scoped ke file fitur) → perbaiki Critical/Important → re-review sampai Ready to merge.

- [ ] **Step 3: Push + verifikasi deploy**

```bash
git pull --rebase origin main && node --test tests/community-pin.test.js && git push origin main
```

Lalu probe (domain produksi = `alhijaz.co`, BUKAN miqot.com):

```bash
curl -s -X POST https://alhijaz.co/api/community/posts/00000000-0000-0000-0000-000000000000/pin -w '\nHTTP %{http_code}\n' --max-time 20
```

Expected setelah webhook deploy: `{"error":"Token required"}` + HTTP 401 (rute hidup). Poll berkala sampai 401 atau timeout ±6 menit.

- [ ] **Step 4: Serah-terima ke user (checklist bangun tidur)**

**A. Migrasi** (Supabase SQL Editor — kode yang sudah ter-deploy aman menunggu ini; sebelum migrasi: blok pin tidak tampil & menu Sematkan → pesan 503):

```sql
ALTER TABLE community_posts ADD COLUMN pinned_at timestamptz;
CREATE UNIQUE INDEX community_posts_single_pin ON community_posts ((true)) WHERE pinned_at IS NOT NULL;
```

**B. E2e:** admin pin → blok "📌 Disematkan" muncul di puncak + baris kronologisnya hilang; pin kiriman lain → menggantikan; lepas sematan → blok hilang, kiriman kembali; akun non-admin tidak melihat menu; hapus kiriman yang dipin → blok hilang setelah muat ulang; badge tampil di halaman detail; refresh setelah tiap langkah.
