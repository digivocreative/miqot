node_modules
dist
dist_staging
dist_old
.DS_Store
.dev.vars
.env
functions/umroh-landing.mjs
functions/haji-landing.mjs
data/*.json
data/kurs-share-cache/
data/backups/
exports/
.superpowers/
.claude/worktrees/

# pre-cutover env backup (old secrets) — keep local, never commit
.env.cutover-bak

# semua varian/backup .env — repo ini publik, jangan pernah ter-commit
.env.*
!.env.example

# runtime log artifact
server.log
